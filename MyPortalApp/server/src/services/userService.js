const { createUser } = require("../controllers/user.controller");

import * as UserDao from "../repositories/userRepo"
export const  createUser = _createUser

function _createUser(){
   return new Promise ((resolve, reject) => {
       
   })
}